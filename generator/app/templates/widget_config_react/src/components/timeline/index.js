import React, { PropTypes } from "react";
import { Icon, Timeline }  from 'antd';
import './index.less';

export default class EnhancerTimeline extends React.Component {
  static propTypes = {
  }
  static defaultProps = {
  }

  constructor(props) {
    super();

    this.state = {}
  }
  render() {
    const self = this;
   
    return (<div >
        EnhancerTimeline
      </div>
    )
  }
};
